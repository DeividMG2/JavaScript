// Conocidos como arrays, matrices, vectores - Almacena multiples valores

var amigos = ["Carlos","Cesar","Alex",true,12,7.6,"Sofia"];

// document.write(amigos[2]) //Para imprimir el elemento de ese index

// document.write(amigos) // Para imprimir todo el arreglo

// amigos[0]="Arturo"; //Establece un nuevo valor al primer elemento en la posicion 0 del arreglo


//Metodos y Propiedades de Arreglos

// tam=amigos.length //Para acceder a la longitud del arreglo

// amigos[amigos.length]="Fernando"; //Forma de agregar un elemento al final

// amigos.push("Ultimo elemento"); //Metodo para agregar uno o más elementos al final

// amigos.pop(); //Metodo para eliminar el último elemento del arreglo

// var amigos2 = ["Luis","Fernando","Oscar"]

// var amigos3 = amigos.concat(amigos2) //Se concatena el arreglo amigos2 al arreglo amigos (se unen)

// amigos = amigos.join(" : ") //Separa los elementos con lo que le mandemos de parametro

// var ordenados = amigos.sort() //Ordena los elementos del arreglo alfabéticamente
